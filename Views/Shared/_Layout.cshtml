<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - OBS Sınav Sistemi</title>

    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
<<<<<<< HEAD
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
</head>

<body class="bg-light">
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm mb-4">
            <div class="container-fluid">
                <a class="navbar-brand fw-bold" asp-controller="Home" asp-action="Index">
                    <i class="bi bi-calendar-check me-2"></i>Sınav Planlama Sistemi
                </a>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarMain">
                    <ul class="navbar-nav flex-grow-1">
                        <li class="nav-item">
                            <a class="nav-link text-white" asp-controller="Home" asp-action="Index">Anasayfa</a>
                        </li>

                        @if (User.Identity != null && User.Identity.IsAuthenticated)
                        {
                            <li class="nav-item">
                                <a class="nav-link text-white" asp-controller="Ders" asp-action="Index">Dersler</a>
                            </li>

                            <li class="nav-item">
                                <a class="nav-link text-white" asp-controller="Derslik" asp-action="Index">Derslikler</a>
                            </li>

                            <li class="nav-item">
                                <a class="nav-link text-white" asp-controller="Oturum" asp-action="Index">Oturumlar</a>
                            </li>

                            @if (User.IsInRole("Admin"))
                            {
                                <li class="nav-item">
                                    <a class="nav-link text-warning fw-bold" asp-controller="Sinav" asp-action="Olustur">
                                        Yeni Sınav / Salon Ata
                                    </a>
                                </li>

                                <li class="nav-item">
                                    <a class="nav-link text-white" asp-controller="Personel" asp-action="Index">
                                        Personel Yönetimi
                                    </a>
                                </li>
                            }
                        }
                    </ul>

                    <ul class="navbar-nav align-items-center">
                        @if (User.Identity != null && User.Identity.IsAuthenticated)
                        {
                            <li class="nav-item">
                                <span class="nav-link text-white">
                                    <i class="bi bi-person-circle me-1"></i>
                                    @User.Identity.Name (@User.FindFirst(System.Security.Claims.ClaimTypes.Role)?.Value)
                                </span>
                            </li>

                            <li class="nav-item">
                                <a class="nav-link text-white" asp-controller="Account" asp-action="Logout">Çıkış</a>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="nav-link text-white" asp-controller="Account" asp-action="Login">Giriş Yap</a>
                            </li>
                        }
                    </ul>
=======
</head>

<body>
    <div class="app-shell">

        <aside class="sidebar">
            <div class="sidebar-brand">
                <div class="brand-icon">✓</div>
                <div>
                    <strong>Sınav Planlama</strong>
                    <span>OBS Yönetim</span>
>>>>>>> 6e0fdc6 (Sınav yönetimi ve temel CRUD ekranları geliştirildi)
                </div>
            </div>

<<<<<<< HEAD
    <div class="container-fluid px-4">
        <main role="main" class="pb-3">
            @RenderBody()
=======
            <nav class="sidebar-menu">
                <a asp-controller="Home" asp-action="Index" class="menu-link">
                    <span>📊</span>
                    Yönetim Paneli
                </a>

                <a asp-controller="Derslik" asp-action="Index" class="menu-link">
                    <span>🏫</span>
                    Derslik Yönetimi
                </a>

                <a asp-controller="Ders" asp-action="Index" class="menu-link">
                    <span>📚</span>
                    Ders Yönetimi
                </a>

                <a asp-controller="Oturum" asp-action="Index" class="menu-link">
                    <span>🕘</span>
                    Oturum Yönetimi
                </a>

                <a asp-controller="Sinav" asp-action="Index" class="menu-link">
                    <span>🗓️</span>
                    Sınav Planlama
                </a>

                <a asp-controller="GozetmenAtama" asp-action="Index" class="menu-link">
                    <span>👥</span>
                    Gözetmen Atama
                </a>

                @if (User.IsInRole("Admin"))
                {
                    <a asp-controller="SinavLog" asp-action="Index" class="menu-link">
                        <span>🧾</span>
                        Log Kayıtları
                    </a>
                }
            </nav>

            <div class="sidebar-footer">
                <small>Giriş yapan kullanıcı</small>
                <strong>@User.Identity?.Name</strong>

                @if (User.IsInRole("Admin"))
                {
                    <span class="role-pill admin">Admin</span>
                }
                else
                {
                    <span class="role-pill viewer">Viewer</span>
                }
            </div>
        </aside>

        <main class="main-area">
            <header class="topbar">
                <div>
                    <h1>@ViewData["Title"]</h1>
                    <span>Fakülte sınav planlama ve gözetmen yönetim sistemi</span>
                </div>

                <div class="topbar-actions">
                    <a asp-controller="Home" asp-action="Index" class="topbar-btn">
                        Panel
                    </a>

                    <a asp-controller="Account" asp-action="Logout" class="topbar-btn danger">
                        Çıkış
                    </a>
                </div>
            </header>

            <section class="content-area">
                @RenderBody()
            </section>
>>>>>>> 6e0fdc6 (Sınav yönetimi ve temel CRUD ekranları geliştirildi)
        </main>

    </div>

<<<<<<< HEAD
    <footer class="border-top footer text-muted bg-white mt-auto py-3">
        <div class="container text-center">
            &copy; 2026 - YZM 2126 Sınav Planlama ve Gözetmen Atama Sistemi
        </div>
    </footer>

=======
>>>>>>> 6e0fdc6 (Sınav yönetimi ve temel CRUD ekranları geliştirildi)
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>